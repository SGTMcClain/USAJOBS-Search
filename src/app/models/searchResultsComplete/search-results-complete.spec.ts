import { SearchResultsComplete } from './search-results-complete';

describe('SearchResultsComplete', () => {
  it('should create an instance', () => {
    expect(new SearchResultsComplete()).toBeTruthy();
  });
});
